services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    working_dir: /workspace
    volumes:
      - .:/workspace
      - vcpkg-data:/opt/vcpkg
    environment:
      VCPKG_ROOT: /opt/vcpkg
    stdin_open: true
    tty: true

volumes:
  vcpkg-data:
